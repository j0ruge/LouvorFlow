# Server
HOST=::
PORT=8080

# Porta do backend para o proxy de desenvolvimento (deve corresponder ao PORT do backend).
# API_PORT=3000

# URL absoluta da API. Opcional em desenvolvimento (o proxy do Vite encaminha /api).
# OBRIGATÓRIO em produção — sem reverse proxy, requisições a /api retornarão 404.
# VITE_API_BASE_URL=http://localhost:3000/api
